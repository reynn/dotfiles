#!/usr/bin/env fish

# --- file.extract ---

complete -c 'file.extract' -d 'The archive to extract' -l 'archive' -s 'a'
complete -c 'file.extract' -d 'The target directory to extract files to' -l 'dest or destination' -s 'd'
