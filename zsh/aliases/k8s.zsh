#!/usr/bin/env zsh

# -----------------------------------------------------------------------------
# Stern (k8s logging) ---------------------------------------------------------

alias sterns='stern'
alias stern='stern -E linkerd-proxy'
