# ------------
# Strings
bins_dir: "{{ ansible_env.HOME }}/.bins"
git_path: "{{ ansible_env.HOME }}/git"
dotfiles_path: "{{ git_path }}/reynn/dotfiles"

# ------------
# Maps
packages:
  system:
    - zsh
    - git
    - curl
    - wget
    - tmux
  pip:
    - certifi
    - Pygments
    - yq

# ------------
# Lists
bin_downloads:
  - name: Ripgrep
    git:
      owner: BurntSushi
      repo: ripgrep
    alias: rg
  - name: JQ
    git:
      owner: stedolan
      repo: jq
  - name: exa
    git:
      owner: ogham
      repo: exa
  - name: skaffold
    git:
      owner: GoogleContainerTools
      repo: skaffold
  - name: goreleaser
    git:
      owner: goreleaser
      repo: goreleaser
  - name: minikube
    git:
      owner: kubernetes
      repo: minikube
  - name: rio
    git:
      owner: rancher
      repo: rio
  - name: doctl
    git:
      owner: digitalocean
      repo: doctl
  # - name: k9s # Commented out, all releases currently marked as pre-release
  #   git:
  #     owner: derailed
  #     repo: k9s
  - name: stern
    git:
      owner: wercker
      repo: stern
git_repos:
  - repo: git@github.com:reynn/dotfiles.git
    dest: "{{ git_path }}/reynn/dotfiles"
  - repo: git@github.com:reynn/k8s.git
    dest: "{{ git_path }}/reynn/k8s"
  - repo: git@github.com:thecasualcoder/kube-fzf.git
    dest: "{{ git_path }}/thecasualcoder/kube-fzf"
  - repo: git@github.com:robbyrussell/oh-my-zsh.git
    dest: "{{ git_path }}/robbyrussell/oh-my-zsh"
  - repo: git@github.com:bhilburn/powerlevel9k.git
    dest: "{{ git_path }}/robbyrussell/oh-my-zsh/custom/themes/powerlevel9k"
  - repo: git@github.com:mbadolato/iTerm2-Color-Schemes.git
    dest: "{{ git_path }}/mbadolato/iterm2-color"
  - repo: git@github.com:powerline/fonts.git
    dest: "{{ git_path }}/powerline/fonts"
  - repo: git@github.com:junegunn/fzf.git
    dest: "{{ git_path }}/junegunn/fzf"
commands:
  - cmd: "go build -o {{ bins_dir }}/helpers main.go"
    dir: "{{ git_path }}/reynn/dotfiles/go/helpers"
scripts:
  - name: gimme
    url: https://raw.githubusercontent.com/travis-ci/gimme/master/gimme
    args: stable
  - name: nvm
    url: https://raw.githubusercontent.com/nvm-sh/nvm/v0.34.0/install.sh
    args: stable
